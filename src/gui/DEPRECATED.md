# DEPRECATED — src/gui (legacy)

Esta carpeta contiene el prototipo GUI anterior (`window.py` monolítico).

**Canónica:** `triplewrapper_gui/` en la raíz del repo
(módulos `views/`, `widgets/`, `core/bridge.py`, `meson.build`, `pyproject.toml`).

No borrar todavía: sirve como referencia para `QueuePanelWidget`
(ya copiado a `triplewrapper_gui/widgets/queue_panel.py`).

Próximo paso: eliminar `src/gui/` cuando la GUI canónica cubra
encolar/pausar/reanudar desde el panel (v0.2.2).
